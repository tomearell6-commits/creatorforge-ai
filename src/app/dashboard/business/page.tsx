import { ExecDashboard } from "@/components/business/ExecDashboard";

export const metadata = { title: "Executive Dashboard — Business — CreatorsForge AI" };

export default function BusinessHomePage() {
  return <ExecDashboard />;
}
