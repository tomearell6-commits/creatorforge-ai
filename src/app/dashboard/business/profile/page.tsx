import { CompanyProfileManager } from "@/components/business/CompanyProfileManager";

export const metadata = { title: "Company Profile — Business — CreatorsForge AI" };

export default function BusinessProfilePage() {
  return <CompanyProfileManager />;
}
