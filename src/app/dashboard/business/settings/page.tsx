import { BusinessSettings } from "@/components/business/BusinessSettings";

export const metadata = { title: "Business Settings — CreatorsForge AI" };

export default function BusinessSettingsPage() {
  return <BusinessSettings />;
}
